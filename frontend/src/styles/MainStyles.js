//Aqui se establece unos estilos generales para lo que es el main

const mainStyles = {
    flexGrow: 1, // Permite que el main crezca según el contenido
    padding: "20px", // Espaciado interno
    minHeight: "calc(100vh - 64px)", // Ajuste para evitar cortes en pantallas pequeñas
    backgroundColor: "#f5f5f5", // Color de fondo suave
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
  };
  
  export default mainStyles;
  